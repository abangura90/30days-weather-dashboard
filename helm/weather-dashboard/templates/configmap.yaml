---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "weather-dashboard.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "weather-dashboard.labels" . | nindent 4 }}
data:
  s3-bucket-name: {{ .Values.aws.s3Bucket | quote }}